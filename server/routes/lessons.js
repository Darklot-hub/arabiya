import {Router} from 'express'; import * as c from '../controllers/lessons.js'; import {auth} from '../middleware/auth.js'; const r=Router();r.get('/',c.list);r.get('/:id',c.get);r.post('/',auth,c.create);r.put('/:id',auth,c.update);r.delete('/:id',auth,c.remove);export default r;
